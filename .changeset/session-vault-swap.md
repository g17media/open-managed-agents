---
"@openma/sdk": minor
---

Sessions: `update()` (title / metadata / `vault_ids`) and per-interaction vault swapping — pass `vault_ids` to `message()`, `chat()`, or `chatComplete()` to switch the session's credential set before the turn dispatches.
