---
"@openma/sdk": minor
---

Agents: bring `CreateAgentInput`/`UpdateAgentInput` to wire parity — `callable_agents`/`multiagent`, `harness`, `enable_general_subagent`, optimistic-concurrency `version`, and the `_oma` envelope (`aux_model`, `runtime_binding`, `appendable_prompts`).
